# Exemplo de variáveis de ambiente (NÃO COMITE valores reais)
# Copie para .env e preencha com seus valores locais.

# --- Backend (Node.js / Railway) ---
PORT=3000

# Firebase Admin SDK
# Use apenas UM dos métodos abaixo:
# 1) JSON da conta de serviço como string (recomendado no Railway)
FIREBASE_SERVICE_ACCOUNT=<JSON_STRING_OF_SERVICE_ACCOUNT>
# 2) Ou projeto para Application Default Credentials
FIREBASE_PROJECT_ID=<YOUR_FIREBASE_PROJECT_ID>

# SMTP (para envio de e-mails)
# Escolha um provedor e preencha os placeholders.
SMTP_SERVICE=<gmail|hotmail|smtp>
SMTP_HOST=<YOUR_SMTP_HOST>
SMTP_PORT=<YOUR_SMTP_PORT>
SMTP_USER=<YOUR_SMTP_USERNAME>
SMTP_PASS=<YOUR_SMTP_PASSWORD>

# --- Observações ---
# - Este arquivo é apenas um modelo. Crie um arquivo .env com seus valores.
# - Garanta que .env esteja listado no .gitignore para evitar vazamento de segredos.# ============================================
# CONFIGURAÇÃO DO BACKEND RAILWAY
# ============================================

# URL do backend Railway (obrigatório)
BACKEND_URL=https://tcc-carona-universit-rio-production.up.railway.app

# ============================================
# CONFIGURAÇÃO SMTP (para envio de e-mails)
# ============================================
# Configure estas variáveis no Railway para enviar e-mails reais

# Provedor SMTP: gmail, outlook, etc
SMTP_SERVICE=gmail

# E-mail que enviará os códigos
SMTP_USER=seu-email@gmail.com

# Senha de aplicativo (não use sua senha normal!)
# Gmail: https://myaccount.google.com/apppasswords
SMTP_PASS=sua-senha-de-app

# ============================================
# PROVEDOR DE E-MAIL (opcional - fallback)
# ============================================

# Provedor: 'emailjs', 'resend' ou 'mailgun'
# Padrão: Railway Backend (automático)
EMAIL_PROVIDER=emailjs

# EmailJS (gratuito até 200 emails/mês)
EMAILJS_SERVICE_ID=
EMAILJS_TEMPLATE_ID=
EMAILJS_PUBLIC_KEY=
EMAILJS_PRIVATE_KEY=

# Resend (gratuito até 3000 emails/mês)
RESEND_API_KEY=

# Mailgun (gratuito até 5000 emails/mês)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=

# ============================================
# FIREBASE (configurado via firebase_options.dart)
# ============================================
# Não precisa configurar aqui
