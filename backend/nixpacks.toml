[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server.js"

# Força detecção do provider Node de forma explícita (Nixpacks espera uma lista, não um mapa)
providers = ["node"]
