org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
android.overridePathCheck=true

# Configurações para reduzir avisos de compilação
org.gradle.warning.mode=summary

# Suprime avisos sobre Java 8 obsoleto (vindos de dependências)
# Os avisos são normais quando dependências usam Java 8, mas o projeto usa Java 17
# Isso é seguro e não afeta a funcionalidade